node_modules
/build
/.svelte-kit
/package
.env
.env.*
!.env.example
vite.config.ts.timestamp-*
.DS_Store
.wrangler
.dev.vars

# Worktree-local cache symlink (accidentally committed once)
.cache

# design-ctx scan snapshot (local diffing only)
.design-ctx.snapshot.json
